public class Bullet {
    // x < bullet.getBulletX() + bullet.getBulletWidth() &&
    //x + enemy.enemyWidth > bullet.getBulletX() &&
    //y < bullet.getBulletY() + bullet.getBulletHeight() &&
    //y + enemy.enemyHeight > bullet.getBulletY();

    // tambahin fungsi yang bullet.
}
